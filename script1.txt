снять NOT NULL у поля Creator

/*-------------------------------------------------*/


SET TERM ^ ;

ALTER PROCEDURE DOCMATERIALPERMITINSERT (
    ID INTEGER,
    UNITID INTEGER,
    DOCNUMBER VARCHAR(50),
    DOCDATE DATE,
    PERSON VARCHAR(300),
    VEHICLEMARK VARCHAR(100),
    LICENSEPLATE VARCHAR(50),
    CREATEFLAG SMALLINT,
    CREATOR INTEGER,
    CREATEDATE DATE,
    SIGNEDFLAG SMALLINT,
    SIGNER INTEGER,
    SIGNDATE DATE,
    ENTRYFLAG SMALLINT,
    ENTRYPOINT INTEGER,
    ENTRYDATE DATE,
    COMMENT BLOB SUB_TYPE 1 SEGMENT SIZE 4096,
    MODIFIER INTEGER)
AS
BEGIN
  INSERT INTO DOCMATERIALPERMIT (
    ID,
    UNITID,
    DOCNUMBER,
    DOCDATE,
    PERSON,
    VEHICLEMARK,
    LICENSEPLATE,
    CREATEFLAG,
    CREATOR,
    CREATEDATE,
    SIGNEDFLAG,
    SIGNER,
    SIGNDATE,
    ENTRYFLAG,
    ENTRYPOINT,
    ENTRYDATE,
    COMMENT,
    MODIFIER,
    MODIFYDATE)
  VALUES (
    :ID,
    :UNITID,
    :DOCNUMBER,
    :DOCDATE,
    :PERSON,
    :VEHICLEMARK,
    :LICENSEPLATE,
    :CREATEFLAG,
    :CREATOR,
    :CREATEDATE,
    :SIGNEDFLAG,
    :SIGNER,
    :SIGNDATE,
    :ENTRYFLAG,
    :ENTRYPOINT,
    :ENTRYDATE,
    :COMMENT,
    :modifier,
    current_timestamp);
END

^

SET TERM ; ^

/*-------------------------------------------------*/